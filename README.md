# Clone zip về giải nén
- Git Bash Here:
 + `nmp install` xong ngồi đợi =)))
 + `cd mongodb`: chạy lần lượt
    `node createdb`
    `node insertvalue`
    đã tạo xong database =)))
 + `cd ../` quay lại TodoApp, gõ `node index` để chạy server
- lần sau chỉ cần chạy `node index`